name: plugin
version: '${version}'
main: dev.geri.shops.Shops
api-version: '1.20'
commands:
  shops:

permissions:
  shops.help:
    description: Show the help message
    default: op
  
  shops.reload:
    description: Reload the plugin's config at runtime
    default: op
